// Build-time switches.

// When true, the page reads playtest parameters from the URL (see
// docs/playtest.md). Set to false for a release build: every parameter is
// then ignored and the game always starts normally.
export const PLAYTEST_URL = true;
